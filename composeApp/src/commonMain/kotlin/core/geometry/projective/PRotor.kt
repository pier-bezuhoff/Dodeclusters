package core.geometry.projective

// scalar + bivector
class PRotor {
    companion object {
        // translation, rotation, scaling
        // Q: find out how to do perspective transformations
    }
}